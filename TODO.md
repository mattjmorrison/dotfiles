# TODO

## Neovim

- **Terminal `<C-/>` working directory**: When opening a terminal with `<C-/>` inside LazyVim, the terminal's cwd should always be the project root (not the directory of the current buffer). Needs investigation into how Snacks terminal sets cwd and whether there's a hook to override it.
